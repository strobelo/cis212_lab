## Material

This week we finished `cat` with `"bns"` options. We talked about `make` and principles of writing Makefiles; we talked about compiling and linking, and what `make` does. We finally went over `gdb`and talked about its basics.  
